# Partner Intelligence Suite

> Telegram-бот для проверки бизнес-партнёров через AI

Введите ИНН компании — получите финансовый анализ, оценку рисков и готовый Word-отчёт. Всё на базе Mistral AI с кэшированием в Redis.

## Что умеет

- Ищет партнёров по ИНН или названию
- Анализирует финансы и риски через Mistral AI
- Генерирует Word-отчёты
- Показывает историю оборотов
- Кэширует результаты (быстрые повторные запросы)
- Собирает статистику использования

## Быстрый старт

**1. Скопируйте `.env.example` в `.env`:**
```bash
cp .env.example .env
```

**2. Заполните `.env`:**
```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
MISTRAL_API_KEY=ваш_ключ_mistral
MYSQL_ROOT_PASSWORD=придумайте_пароль
MYSQL_PASSWORD=ещё_один_пароль
```

Токены:
- Telegram — [@BotFather](https://t.me/botfather)
- Mistral — [console.mistral.ai](https://console.mistral.ai)

**3. Запустите через Docker:**
```bash
docker-compose up -d
```

**4. Проверьте:**
```bash
docker-compose logs -f bot
```

## Как пользоваться

1. Напишите боту `/start`
2. Отправьте ИНН компании (например: `7707049388`)
3. Получите инфо и выберите:
   - 🤖 AI-анализ — финансы, риски, рекомендации
   - 📄 Word-отчёт — готовый документ
   - 📊 Финансы — история оборотов
   - 📞 Контакты — реквизиты

## Тестовые ИНН

- `7707049388` — Global Tech Solutions
- `7830002293` — Eco Manufacturing  
- `5001007322` — Logistics Worldwide

## Что внутри

- **MySQL** — база партнёров и история запросов
- **Redis** — кэш для быстрых ответов
- **Mistral AI** — анализ и рекомендации
- **phpMyAdmin** — смотреть базу на `localhost:8080`

## Команды

```
/start - Начать
/help - Помощь
/search название - Найти по имени
/stats - Статистика бота
/health - Проверка всех систем
```

## Если что-то сломалось

**Бот не стартует:**
```bash
docker-compose logs bot
```

**Проблема с MySQL:**
```bash
docker-compose logs mysql
docker-compose restart mysql
```

**Redis не работает:**
```bash
docker-compose restart redis
```

**Полная перезагрузка:**
```bash
docker-compose down
docker-compose up -d
```

## Структура

```
├── app/
│   ├── bot.py              # Код бота
│   ├── database.py         # Работа с MySQL
│   ├── mistral_analyzer.py # AI-анализ
│   └── report_generator.py # Генерация Word
├── docker-compose.yml      # Настройки Docker
├── .env                    # Ваши токены
└── logs/                   # Логи
```

## Требования

- Docker и Docker Compose
- 4GB RAM минимум



MIT License
